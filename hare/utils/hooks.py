"""
User-defined lifecycle shell hooks (`hooks.ts`).

The TypeScript module is very large; subprocess spawning and hook JSON schemas
live in ``hare.tools`` / services. Import those packages when wiring execution.
"""

from __future__ import annotations
