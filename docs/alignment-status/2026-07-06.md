# Alignment Status - 2026-07-06

## 当前结论

- E2E 主链路当前是绿的。
- 旧 Phase1 对齐链路当前已经收口到 `legacy_alignment/` 并可独立运行。
- 当前最需要继续推进的，是收口重复资产边界、补真差分覆盖，以及处理 mypy 质量门失效。
- 如果后续要真正退出 root `alignment/` 镜像层，当前条件清单见 `docs/alignment-status/2026-07-06-demirroring-checklist.md`。

## 当前验证结果

### E2E

```bash
python -m pytest tests/e2e -q
```

结果：

- `64 passed`

### transcript roundtrip

```bash
python -m pytest tests/test_session_persistence_roundtrip.py -q
```

结果：

- `2 passed`

### 静态质量

```bash
python scripts/check_mypy_regression.py --baseline 210
python scripts/detect_stubs.py
```

结果：

- mypy：`1011 errors`
- stub：`12 x NotImplementedError`, `15 x TODO/FIXME`

## 当前目录职责

- `hare/alignment/`：当前 golden E2E 主资产目录
- `legacy_alignment/`：旧 519-case Phase1 / py-only 体系
- root `alignment/`：兼容镜像层，不应继续作为新 E2E 资产主目录

## 当前仍存在的问题

### 1. 资产重复与边界不够清晰

- 当前仓库里仍同时存在 `hare/alignment/`、`legacy_alignment/` 和 root `alignment/`。
- root / `hare/` 双层代码树会继续制造路径歧义，也会放大 stub 计数和维护成本。

### 2. 真差分覆盖仍然偏薄

- 当前能证明的主要还是主链路 E2E 已恢复。
- 更高风险的行为轴还需要继续补 case：
  - `--resume` / `--continue`
  - subagent / Agent
  - hooks
  - MCP
  - permission modes × settings
  - compact / auto-compact
  - transcript JSONL schema

### 3. mypy 质量门失效

- 当前基线 `210` 与真实结果 `1011` 已明显脱节。
- 不先重设或分模块治理，这个门基本没有实际约束力。

## 当前建议顺序

1. 继续收口 `hare/alignment/`、`legacy_alignment/`、root `alignment/` 的职责边界。
2. 优先补真差分覆盖，不继续把 py-only case 当作主要进度指标。
3. 明确 mypy 是先重设基线，还是直接分模块清偿。
