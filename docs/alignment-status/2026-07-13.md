# Alignment Status - 2026-07-13

## MCP runtime

- Committed explicit `--mcp-config` loading and stdio server lifecycle wiring in `7d86240c`.
- MCP tools are exposed to the query tool pool as `mcp__<server>__<tool>` objects.
- Stdio JSON-RPC reading was changed to line-oriented reads so small responses complete the handshake.
- Runtime adapter coverage is provided by `tests/test_mcp_runtime.py`.
- The explicit discovery lifecycle test now passes: `2 passed` in
  `tests/test_mcp_runtime.py` (commit `5e6320b4`).

## Blocked oracle

- The planned `mcp.stdio_tool_call` TS differential recording was attempted with the recovered CLI and local mock server, but the process hung and produced no golden.
- The unverified case is therefore not included in the main E2E case set. The echo fixture/config seed remain available for retry after the TS oracle is repaired.

## Permission modes and settings

- CLI turns now build one explicit `ToolPermissionContext` from project/local/user
  `.hare/settings*.json`, `--allowed-tools`, `--disallowed-tools`, and
  `--permission-mode`.
- The context is passed through `HareClient`/`QueryEngine` into the permission
  engine, so deny/ask/allow rules and `bypassPermissions` are exercised by the
  same tool-call path as print, resume, and REPL modes.
- Request-side regression coverage is in `tests/test_permission_context_cli.py`:
  settings/CLI merge, deny-rule enforcement, and bypass-with-deny precedence.
- Python-side coverage is implemented and verified; a true TS differential
  golden for the permission/settings axis is still pending and remains an
  oracle blocker rather than an alignment claim.

Verification:

```text
python -m pytest tests/test_permission_context_cli.py tests/test_resume_seeds_messages.py hare/tests/test_hare_print_handler.py hare/tests/test_hare_cli_integration.py -q  # 17 passed
python scripts/check_mypy_regression.py --baseline 520  # PASS, 520 == 520
```

## Hooks runtime boundary

- Registered `AsyncHookRegistry` callbacks now execute as Python callbacks in
  the tool-hook pipeline; they are no longer misinterpreted as shell command
  names.
- `hookSpecificOutput.permissionDecision` is normalized to the
  `permissionBehavior` field consumed by PreToolUse resolution.
- `tests/test_tool_hooks_runtime.py` verifies a registered PreToolUse callback
  produces a deny decision.
- This is request-side/runtime coverage only; the planned TS hook goldens are
  still not recorded, so the parity matrix remains `implemented-unverified`.
