# Alignment Status - 2026-07-13

## MCP runtime

- Committed explicit `--mcp-config` loading and stdio server lifecycle wiring in `7d86240c`.
- MCP tools are exposed to the query tool pool as `mcp__<server>__<tool>` objects.
- Stdio JSON-RPC reading was changed to line-oriented reads so small responses complete the handshake.
- Runtime adapter coverage is provided by `tests/test_mcp_runtime.py`.

## Blocked oracle

- The planned `mcp.stdio_tool_call` TS differential recording was attempted with the recovered CLI and local mock server, but the process hung and produced no golden.
- The unverified case is therefore not included in the main E2E case set. The echo fixture/config seed remain available for retry after the TS oracle is repaired.
