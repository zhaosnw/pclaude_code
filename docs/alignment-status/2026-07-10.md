# Alignment Status - 2026-07-10

## Completed

- `1e0e2997 feat: add session and MCP alignment coverage`
  - Added multi-invocation session recording and lifecycle cases for
    `--resume` and `--continue`.
  - Added the MCP invalid-config differential and matched the TS exit/stderr
    behavior.
  - Added a reusable stdio MCP echo seed.
- `5436df23 chore: track verified session parity`
  - The generated parity matrix now retains evidence for verified `--resume`
    and `--continue` behavior.

## Verification

- `python -m pytest tests/e2e -q`: `68 passed, 1 xfailed`.
- `make alignment-guardrails`: `16 passed`.
- `python scripts/check_mypy_regression.py --baseline 520`: passed.

## Dogfood

- File-change scenario passed in an isolated temporary git repository with
  isolated config directories.
  - Fixture: `hare/alignment/fixtures/dogfood_read_edit.json`.
  - Workflow: `Read(edit_src.txt)` then `Edit(world -> there)`.
  - Result: exit `0`, final content `hello there`.

## Outstanding

- `session.resume_bad_id` remains an xfail: the recovered TS print-mode entry
  silently succeeds for a missing session ID, while hare intentionally emits a
  visible error.
- The recovered TS CLI hangs during a settings-authorized tool-call recording,
  so the permission/settings success case is not yet eligible for a golden.
