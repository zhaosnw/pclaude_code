# Alignment Status - 2026-07-07

## 当前结论

- E2E 主链路当前仍是绿的。
- 旧 Phase1 对齐链路当前仍收口在 `legacy_alignment/` 并可独立运行。
- 过去一天的主要进展不是代码行为变化，而是把 repo-root、`hare/`、`legacy_imports/` 这几层高可见度文档口径统一到了同一套当前结构说明。
- 当前仍需要继续推进的主线没有变化：收口重复资产边界、补真差分覆盖、处理 mypy 质量门失效。

## 当前验证基线

当前仍以最近一次已复核结果为准：

- `python -m pytest tests/e2e -q` → `64 passed`
- `python -m pytest tests/test_session_persistence_roundtrip.py -q` → `2 passed`
- `python scripts/check_mypy_regression.py --baseline 210` → `1011 errors`
- `python scripts/detect_stubs.py` → `12 x NotImplementedError`, `15 x TODO/FIXME`

这次 2026-07-07 的更新没有引入代码逻辑修改，因此没有为纯文档收口再次重复运行同一批测试。

## 今天完成的文档收口

当前这些入口文件已经明确区分“当前状态页”和“历史计划 / 历史台账”：

- `README.md`
- `REVIEW_2026-07-02.md`
- `docs/alignment-status/README.md`
- `docs/alignment-findings.md`
- `hare/docs/alignment-findings.md`
- `docs/superpowers/plans/2026-07-02-hare-completion-and-architecture-roadmap.md`
- `hare/docs/superpowers/plans/2026-07-02-hare-completion-and-architecture-roadmap.md`
- `docs/superpowers/plans/2026-06-13-e2e-differential-testing.md`
- `hare/docs/superpowers/plans/2026-06-13-e2e-differential-testing.md`
- `alignment/README.md`
- `hare/alignment/README.md`
- `legacy_alignment/README.md`
- `legacy_imports/claude-code-recover-and-python-reset/README.md`

今天还顺手把一个真实运行入口往前收了一步：

- root / `hare/` 两份 `scripts/e2e_runner.py` 现在都同时接受：
  - 旧的 `alignment/fixtures/<name>.json`
  - 更明确的 `hare/alignment/fixtures/<name>.json`
- 对应的 `tests/test_e2e_runner.py` 与 `hare/tests/test_hare_e2e_runner.py` 已切到使用 canonical 的 `hare/alignment/...` 形式
- 这样新入口可以继续去 root 化，同时运行入口仍兼容历史字符串写法
- 针对这层兼容性已补 focused 覆盖：
  - `python -m pytest tests/test_e2e_runner.py -q` → `4 passed`
  - `python -m pytest hare/tests/test_hare_e2e_runner.py -q` → `4 passed`

同样的兼容规则现在也已经收进 golden 录制入口：

- root / `hare/` 两份 `scripts/record_golden.py` 现在都同时接受：
  - 旧的 `alignment/fixtures/<name>.json`
  - 更明确的 `hare/alignment/fixtures/<name>.json`
- 已补 focused 覆盖：
  - `python -m pytest tests/test_record_golden_paths.py -q` → `1 passed`
  - `python -m pytest hare/tests/test_record_golden_paths.py -q` → `1 passed`

这一步又把真实 E2E case 资产本身往 canonical 路径推进了一截：

- `alignment/cases/**/case.json` 与 `hare/alignment/cases/**/case.json` 中
  所有带 fixture 的 38 个 case，当前都已从
  `alignment/fixtures/<name>.json`
  切到
  `hare/alignment/fixtures/<name>.json`
- 运行入口仍兼容旧字符串：
  - `scripts/e2e_runner.py`
  - `scripts/record_golden.py`
  以及 `hare/` 下对应副本
- 验证结果：
  - `python -m pytest tests/test_alignment_e2e_mirror.py -q` → `1 passed`
  - `python -m pytest tests/e2e/test_e2e_cases.py -q` → `41 passed`
  - `python -m pytest tests/test_alignment_case_fixture_paths.py -q` → `1 passed`
  - `python -m pytest hare/tests/test_hare_alignment_case_fixture_paths.py -q` → `1 passed`

按当前工作树复查后，旧 `alignment/fixtures/...` 字符串在活代码 / 活测试里的剩余位置已经缩到很小：

- 运行入口兼容层：
  - `scripts/e2e_runner.py`
  - `scripts/record_golden.py`
  - 以及 `hare/` 下对应副本
- 兼容性测试：
  - `tests/test_e2e_runner.py`
  - `tests/test_record_golden_paths.py`
  - 以及 `hare/` 下对应副本
- 这层剩余面现在还有 allowlist 守卫：
  - `python -m pytest tests/test_alignment_legacy_fixture_ref_allowlist.py -q` → `1 passed`
  - `python -m pytest hare/tests/test_hare_alignment_legacy_fixture_ref_allowlist.py -q` → `1 passed`
  - root / nested 新守卫现已使用唯一文件名，可在同一 pytest 调用中共同运行

也就是说，当前已经不再有主用 E2E case 资产继续依赖旧前缀；旧前缀主要只剩“平滑迁移用的兼容入口”。

对 repo-root `alignment/` 目录本身的直接活跃依赖面，现在也已有 allowlist 守卫：

- `python -m pytest tests/test_root_alignment_ref_allowlist.py -q` → `1 passed`
- `python -m pytest hare/tests/test_hare_root_alignment_ref_allowlist.py -q` → `1 passed`
- `python -m pytest tests/test_record_golden_paths.py hare/tests/test_hare_record_golden_paths.py tests/test_alignment_case_fixture_paths.py hare/tests/test_hare_alignment_case_fixture_paths.py tests/test_alignment_legacy_fixture_ref_allowlist.py hare/tests/test_hare_alignment_legacy_fixture_ref_allowlist.py tests/test_root_alignment_ref_allowlist.py hare/tests/test_hare_root_alignment_ref_allowlist.py -q` → `8 passed`
- `python -m pytest tests/test_record_golden_paths.py hare/tests/test_hare_record_golden_paths.py tests/test_alignment_case_fixture_paths.py hare/tests/test_hare_alignment_case_fixture_paths.py tests/test_alignment_legacy_fixture_ref_allowlist.py hare/tests/test_hare_alignment_legacy_fixture_ref_allowlist.py tests/test_root_alignment_ref_allowlist.py hare/tests/test_hare_root_alignment_ref_allowlist.py tests/test_e2e_runner.py hare/tests/test_hare_e2e_runner.py -q` → `16 passed`

另一个需要单独看待的维护约束是：`tests/` 与 `hare/tests/` 之间当前仍有大量同名测试文件。

- 按当前工作树扫描，重复 basename 共有 `104` 组。
- 本轮只把“我们新增的 guardrail / e2e_runner 相关测试”改成了唯一文件名，确保这套新守卫现在可以混跑。
- 这不等于整个仓库的 pytest import-file-mismatch 风险已经被全量解决；大范围去重仍是后续单独工作。

按当前守卫口径，允许保留的直接引用主要集中在：

- `tests/test_alignment_e2e_mirror.py`
- `tests/test_alignment_case_fixture_paths.py`
- nested `hare/tests/*` 中原先那几处“看起来像 root 路径”的入口，当前也已经改成了更明确的子项目语义写法

本轮补充验证：

- `python -m pytest hare/tests/e2e/test_smoke_subprocess.py hare/tests/test_print_mode_json.py hare/tests/test_normalize.py hare/tests/e2e/test_file_effects.py -q` → `13 passed`

## 当前目录职责

- `hare/alignment/`：当前 golden E2E 主资产目录
- `legacy_alignment/`：旧 519-case Phase1 / py-only 体系
- root `alignment/`：兼容镜像层，不应继续作为新 E2E 资产主目录
- `legacy_imports/claude-code-recover-and-python-reset/`：历史取证包，不是当前活跃 canonical 目录

## 当前仍存在的问题

### 1. 资产重复仍未彻底退出

- root `alignment/` 仍存在并被镜像测试约束。
- root / `hare/` 双层代码树仍会继续制造路径歧义和重复统计。

### 2. 真差分覆盖仍然偏薄

- 当前 golden E2E 主集合仍然偏窄。
- 仍值得继续补的轴包括：
  - `--resume` / `--continue`
  - subagent / Agent
  - hooks
  - MCP
  - permission modes × settings
  - compact / auto-compact
  - transcript JSONL schema

### 3. mypy 质量门仍失效

- 当前基线 `210` 与实测 `1011` 仍明显脱节。

## 下一步建议

1. 开始处理“还有哪些活代码 / 活测试仍默认依赖 root `alignment/`”这个收口问题。
2. 继续补真差分轴，而不是只做文档卫生。
3. 重新定义 mypy 基线或直接进入分模块清偿。
